import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-redirect-links',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './redirect-links.component.html',
  styleUrl: './redirect-links.component.scss'
})
export class RedirectLinksComponent {

}
